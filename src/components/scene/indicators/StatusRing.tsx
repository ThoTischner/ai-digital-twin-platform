/**
 * StatusRing — simplified 2D ring variant of HealthGlow.
 * Re-exports HealthGlow for convenience. Use HealthGlow directly for the full
 * animated 3D ring; use StatusRing when you want a named alias in indicator contexts.
 */
export { HealthGlow as StatusRing } from '../effects/HealthGlow'
